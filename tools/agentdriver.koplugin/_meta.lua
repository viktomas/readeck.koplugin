local _ = require("gettext")
return {
    fullname = _("Agent driver (dev only)"),
    description = _([[Local HTTP control API so an AI agent can drive the emulator. Development only; never ship.]]),
}
